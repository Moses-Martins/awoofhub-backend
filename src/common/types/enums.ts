export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  BUSINESS = 'business',
}

export enum BusinessCategory {
  TECH = 'Tech',
  FOOD = 'Food',
  EVENTS = 'Events',
  SERVICES = 'Services'
}

export enum ApprovalStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected',
}